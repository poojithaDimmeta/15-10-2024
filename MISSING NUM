#include <stdio.h>
void main()
{
    int a[15]={15,14,1,2,3,4,5,6,7,8,10,11,12,13};
    int sum=(15*16)/2;
    int tot=0;
for(int i=0;i<14;i++)
    {
        tot=tot+a[i];
    }
    printf("the missing value is %d",sum-tot);
}
